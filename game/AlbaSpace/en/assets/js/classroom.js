let classroomUser = null;
let state = null;
let roomId = new URLSearchParams(location.search).get("room") || "";
let stopEvents = null;
const app = document.getElementById("app"), phaseLabel = document.getElementById("phaseLabel"), roomBadge = document.getElementById("roomBadge");
const standings = room => AlbaSpace.rank(room).map((player,index) => `<tr><td>${index+1}</td><td><strong>${AlbaGame.esc(player.company || "—")}</strong></td><td>🛰️ ${player.small + player.large}/10</td><td>🎓 ${player.graduates}</td></tr>`).join("");
function classroomViewKey(snapshot) {
  return JSON.stringify({ phase:snapshot?.phase || "", round:snapshot?.round || 0, code:snapshot?.code || "", question:snapshot?.currentQuestion ? { id:snapshot.currentQuestion.id, text:snapshot.currentQuestion.text, topicLabel:snapshot.currentQuestion.topicLabel, unit:snapshot.currentQuestion.unit } : null, results:snapshot?.results ? { correct:snapshot.results.correct, explanation:snapshot.results.explanation, items:snapshot.results.items } : null, players:(snapshot?.players || []).map(player => ({id:player.id,company:player.company,ready:player.ready,answered:player.answered,small:player.small,large:player.large,graduates:player.graduates})) });
}
function renderHeader() { phaseLabel.textContent = state ? `${state.phase} · ${state.presentation?.label || AlbaSpace.PRESENTATION_MODES[state.presentationMode]?.label || state.presentationMode}` : "Classroom screen"; roomBadge.textContent = `ROOM ${state?.code || "—"}`; }
function apply(next) { const previousKey = classroomViewKey(state); state = next; if (previousKey !== classroomViewKey(next) || !document.querySelector("#app > *")) render(); else renderHeader(); }
function connect() { stopEvents?.(); stopEvents = AlbaGame.subscribe(roomId, next => apply(next), status => { if (status === "offline") phaseLabel.textContent = "Connection lost. Reconnecting…"; }); }
function render() {
  renderHeader();
  if (!state) { app.innerHTML = `<section class="card hero"><div class="phase">Connection</div><h1 class="classroom-title">📺 Alba Space</h1><p class="muted">Open the Classroom link provided by the teacher.</p></section>`; return; }
  if (state.phase === "QUESTION" && !state.currentQuestion) { app.innerHTML = `<section class="card hero"><h2>Waiting for room data…</h2></section>`; return; }
  if (state.phase === "RESULT" && (!state.currentQuestion || !state.results?.items)) { app.innerHTML = `<section class="card hero"><h2>Waiting for the question result…</h2></section>`; return; }
  if (state.phase === "LOBBY") { app.innerHTML = `<section class="card hero"><div class="phase">${AlbaGame.esc(state.presentation?.label || AlbaSpace.PRESENTATION_MODES[state.presentationMode]?.label || state.presentationMode)}</div><h1 class="classroom-title">Room code</h1><div class="code">${state.code}</div><p class="classroom-kpi">Connected players: <strong>${state.players.length}/10</strong></p><div class="kpis" style="justify-content:center">${state.players.map(player => `<span class="kpi">${player.ready ? "✅" : "⏳"} ${AlbaGame.esc(player.company || "Setting up…")}</span>`).join("")}</div></section>`; return; }
  if (state.phase === "QUESTION") { const question = state.currentQuestion, answered = state.players.filter(player => player.answered).length; app.innerHTML = `<div class="classroom-grid"><section class="card hero"><div class="phase">Question ${state.round} · ${AlbaGame.esc(question.topicLabel)}</div><h1 class="classroom-question">${AlbaGame.esc(question.text)}</h1>${question.unit ? `<p class="classroom-kpi muted">Answer unit: <strong>${AlbaGame.esc(question.unit)}</strong></p>` : ""}</section><section class="card center"><div class="phase">Answers</div><h1 class="classroom-title">${answered}/${state.players.length}</h1><p class="muted">players have answered</p><div class="progress"><span style="width:${state.players.length ? answered/state.players.length*100 : 0}%"></span></div><div class="sep"></div><p class="muted">The correct answer appears after the teacher closes answers.</p></section></div>`; return; }
  if (state.phase === "RESULT") { const question = state.currentQuestion, result = state.results, winners = result.items.filter(item => item.isWinner).map(item => item.company); app.innerHTML = `<div class="classroom-grid"><section class="card hero"><div class="phase">${AlbaGame.esc(question.topicLabel)} · Correct answer</div><div class="classroom-answer">${AlbaGame.esc(result.correct)}${question.unit ? ` ${AlbaGame.esc(question.unit)}` : ""}</div><div class="notice" style="margin-top:22px;font-size:clamp(1.2rem,2vw,2rem)">🤖 ${AlbaGame.esc(result.explanation)}</div>${winners.length ? `<h2 style="margin-top:24px">🏆 Best answer: ${winners.map(AlbaGame.esc).join(", ")}</h2>` : ""}</section><section class="card"><div class="phase">Current ranking</div><table class="table classroom-kpi"><thead><tr><th>#</th><th>Company</th><th>Station</th><th>🎓</th></tr></thead><tbody>${standings(state)}</tbody></table></section></div>`; return; }
  if (state.phase === "ENDGAME") { const ranked = AlbaSpace.rank(state), winner = state.winnerId ? state.players.find(player => player.id === state.winnerId) : ranked[0]; app.innerHTML = `<section class="card hero"><div class="phase">Final</div><h1 class="classroom-title">🏆 ${AlbaGame.esc(winner?.company || "Game over")}</h1><p class="classroom-kpi">${state.winnerId ? "Station complete — 10/10 modules!" : "Session finished."}</p></section><section class="card" style="margin-top:20px"><table class="table classroom-kpi"><thead><tr><th>#</th><th>Company</th><th>Station</th><th>🎓</th></tr></thead><tbody>${standings(state)}</tbody></table></section>`; return; }
  app.innerHTML = `<div class="classroom-grid"><section class="card hero"><div class="phase">Station phase</div><h1 class="classroom-title">🛰️ Build your stations</h1><p class="classroom-kpi muted">Victory: 10/10 modules.</p></section><section class="card"><div class="phase">Station race</div><table class="table classroom-kpi"><thead><tr><th>#</th><th>Company</th><th>Station</th><th>🎓</th></tr></thead><tbody>${standings(state)}</tbody></table></section></div>`;
}
(async () => { classroomUser = await AlbaGame.currentUser(); if (!classroomUser) return AlbaGame.requireLogin(app); if (!roomId) return render(); try { const response = await AlbaGame.snapshot(roomId); apply(response.state); connect(); } catch (error) { app.innerHTML = `<section class="card center"><h2>${AlbaGame.esc(error.message)}</h2></section>`; } })();
